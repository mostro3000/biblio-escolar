# Secretos y datos — NUNCA subir
.env
.env.local
*.pem
/data/
/backups/
vapid_private.pem
# Datos personales / padrones (no se distribuyen)
lista.txt
pof.txt
docentes*.txt
# QR con el dominio real de una escuela (se generan por instalación)
web/qr.png
web/qr-cartel.png

# Entorno y artefactos de build
venv/
__pycache__/
*.pyc
*.tar.gz
*.deb
.DS_Store
